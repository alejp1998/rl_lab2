[project]
name = "rl-lab2"
version = "1.1.0"
description = "Reinforcement Learning Lab 2 — Deep Q-Network on LunarLander-v2 (PyTorch)."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "numpy<2",
    "torch",
    "gym==0.25.2",
    "tqdm",
]

[project.optional-dependencies]
dev = ["pytest", "ruff", "jupyter", "nbconvert"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["slow: long-running training tests (deselect with -m 'not slow')"]

[tool.ruff]
target-version = "py39"
line-length = 100
include = ["tests/**"]  # gate the test suite; 2021 lab code left as-is

